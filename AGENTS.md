# Push Workflow

1. `git checkout main`
2. `git pull origin main`
3. `git checkout -b feature/<short-topic>`
4. Implement, test, and commit your changes
5. `git push -u origin feature/<short-topic>`
6. Open a pull request and merge once reviews/CI pass
